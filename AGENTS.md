# Agent notes for AutoCode

Disk is source of truth.  As little RAM or system resource usage as possible.
Designed for long horizon multi-day/week tasks
Session and persistent project tasks lists

## Project structure
Use the `project_tree` tool call to see the full project file tree (respects .gitignore).
Run `scripts/tree.ps1` as a fallback when `project_tree` is unavailable.
