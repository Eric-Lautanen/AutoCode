[package]
name = "autocode"
version.workspace = true
edition.workspace = true

[[bin]]
name = "autocode"
path = "src/main.rs"

[dependencies]
autocode-ui = { path = "../ui" }

[build-dependencies]
embed-resource = "2.5"
