# AutoCode — Workspace structure

**Mission — A lightweight, egui-based AI coding assistant.**
- Minimize RAM usage and binary size
- Keep codebase clean, organized, maintainable
- Essential features only, no bloat
- Prefer `std`; minimize deps

Cargo.toml (25) — workspace root, 5 crate members (autocode/core/ai/fs/ui), resolver = "2", release LTO/strip, panic=unwind

## crates/autocode/ — binary entry (~494 lines)
main.rs (46) — rustls crypto install, eframe::run_native (1400×900, Glow/Wgpu auto-select), data dir init
app.rs (443) — AutocodeApp (eframe::App): AppState + ChatRuntime map, panel wiring, frame update (sysinfo poll, pending writes, stale session purge, shell task cap at 200, folder picker), floating windows, toolbar/explorer/chat panels, debug inspection, save (sync session state, flush writes, persist project meta), exit (drain runtimes, temp file cleanup)
build.rs (4) — embed Windows icon resource
helpers.rs (1) — reserved

## crates/core/ — state, utilities, tokenizer (~5,019 lines)
lib.rs (18) — crate root, module declarations
state.rs (1376) — AppState, Project, ApiProvider (17 fields: kind, api_key, base_url, model, enabled, max_context_tokens, handoff_percent, allow_project_escape, thinking_mode, reasoning_effort, thinking_api, max_output_tokens, max_output_tokens_thinking, requests_per_hour, models_list_url, saved_models), ProviderKind (OpenRouter/NvidiaNim/OpenAiCompatible/OpenCodeGo), ThinkingApi (Off/DeepSeek/OpenAI), Session (max 50, with id/label/messages/next_message_id/created_at/actual_tokens_used/provider_label/model/todo_list/show_todo/todo_user_dismissed/session_named/handoff_enabled/show_explorer/settings_open/closed/estimated_full_tokens/estimated_messages_tokens), ChatMessage (id/role/content/timestamp/token_count/tool_call_id/tool_calls/tool_meta/reasoning_content/full_content), SecretString (heap-zeroing on drop), TodoItem/TodoList, ProjectTaskList, ProjectMeta (versioned disk metadata), ShellTask/ShellStatus, DesignSettings (72 color fields as [f32;3]), PendingWrites (rate-limited disk batcher), embedded provider/manifest (providers.json via include_str!), DEFAULT_SYSTEM_PROMPT, DEFAULT_HANDOFF_TRIGGER_PROMPT, prune_disk_state, flush_pending_writes (rate-limited by disk_write_rate_ms), model_or_safe fallback
helpers.rs (1449) — ID gen (5-char base36 + hash), token estimation (heuristic: word/symbol/CJK detection, code-vs-prose classification, ~3.2–4.0 chars/token; tiktoken integration via estimate_full_request_tokens), path resolution + traversal guard (resolve_path/ resolve_path_write with cached variants, blocked_error, normalize_path, within_root, find_deepest_existing_ancestor), tiny backtracking regex engine (Pat enum: Lit/Any/Star/Plus/Opt/Class/AnchorStart/AnchorEnd, compile+match, ~L120 of tests), serde defaults (16 functions for AppState fields), budget/usage display (budget_fraction, usage_display, fmt_tokens), unique_data_dir_name, sanitize_filename, update_full_estimate, panic_msg; extensive test suite (regex ~270 lines + token estimation ~130 lines)
fsutil.rs (138) — exe_dir, `\\?\` extended paths (Windows case-sensitivity fix via canonicalize), atomic read_to_string/write/metadata/read_dir/create_dir_all/remove_file/remove_dir/rename/is_dir/display_path (strip \\?\ prefix), write_cmd_script (@echo off wrapper for Windows), TEMP_FILES tracking (OnceLock<Mutex<Vec<PathBuf>>>, temp file register/unregister)
theme.rs (218) — dark Visuals+Style, Palette (20 colors: BG_BASE/PANEL/SURFACE/ACTIVE, ACCENT/ACCENT_DIM, TEXT_PRIMARY/SECONDARY/MUTED/CODE, BORDER, SUCCESS/WARNING/ERROR/PURPLE), hash-based project_accent (djb2→HSV→RGB), ROUND_SM(4)/MD(6)/LG(10), system emoji font support (opt-in via AUTOCODE_EMOJI_FONT=1), apply() sets full egui Visuals+Style
extract.rs (327) — HTML scraping (scraper crate), DuckDuckGo result extraction (decode uddg redirect URLs, domain blacklist of 33 sites), GitHub content extraction (highlight blocks, markdown-body, repo-description), collect_text with tag skipping (script/style/nav/footer/etc.), search cache (LazyLock<Mutex<HashMap>>, 120s TTL, 500 max entries), url_decode, collapse_whitespace
sysinfo.rs (742) — OS/CPU/GPU/RAM/shell/tool detection; Windows: raw Win32 FFI (kernel32 GetSystemInfo+GlobalMemoryStatusEx, advapi32 registry enumeration for CPU name + GPU via PCI vendor scan); Unix: /proc/cpuinfo, /proc/meminfo, sysctl (macOS), /sys/class/drm + lspci (Linux GPU); tool probing (14 tools: rg/grep/git/curl/python/node/cargo/npm/pip/make/docker/findstr/powershell); `has_opengl` (Linux ldconfig/libGL.so path check); hidden subprocess helpers (CREATE_NO_WINDOW on Windows)
session_storage.rs (645) — atomic JSON/JSONL session persistence (temp file + rename via atomic_write), prefix-based load/save/delete (session ID prefix matching for renamed sessions), orphan temp scavenge (cleanup .tmp_* files >1h old), repair_truncated_jsonl_line (salvage corrupt JSONL lines), sanitize_tool_calls (validate+fix tool call arguments, 3 repair strategies), load_messages_before (lazy-load older messages from disk), truncate_messages_after (rewrite JSONL keeping messages up to an ID), project_meta_path/save_project_meta/load_project_meta (versioned ProjectMeta JSON), SessionMeta (separate struct for metadata-only persistence), save_session_meta (metadata-only, JSONL never rewritten from RAM)
tokenizer/mod.rs (107) — Tokenizer trait; TiktokenTokenizer (o200k/cl100k/p50k/gpt2 fallbacks by model family: GPT-4o/o1/o3→o200k, GPT-4/3.5/Claude/Gemini/Llama/Mistral→cl100k, Codex→p50k, GPT-2→gpt2); HeuristicTokenizer fallback; `offline_token_count`

## crates/ai/ — AI provider client + chat orchestration (~6,253 lines)
lib.rs (13) — crate root, module declarations
chat.rs (3385) — orchestration: `send_message`, `start_completion` (pre-flight context check: API counting→tiktoken→heuristic, rate limiting, partial response continuation), SSE stream poll (256 events/frame cap, exponential backoff on idle timeout, request timeout before first byte), error classification (transient: 429/502/503/504/520/timeout/dns/tls/connection errors→retry forever with 5s→180s cap; permanent: auth/quota/content_filter→surface immediately), transient empty-response retry (up to max_retries), tool-call dispatch (18 tool handlers: read_file/read_entire_file/read_files/write_file/list_dir/delete_file/rename_file/create_dir/grep/patch_file/patch_lines/web_search/fetch_url/todo_list/project_task_list/glob/handoff/name_session), tool-call name inference (empty-name recovery from args shape), orphaned tool-call cleanup (walk backwards, remove mismatched tool_calls↔tool_results, up to 3 retries), auto-continuation (on incomplete todo or task-like response, up to max_retries chain), auto-handoff (context threshold check→send trigger prompt→auto-handoff if model doesn't), continuation-chain detection, session auto-naming (stop-word filter, 80-char max), replay (truncate at message + restart), message-truncation (RAM eviction at 2× window), partial response recovery (save interrupted output, prepend on retry), live shell streaming (sequential shell execution with timeout, output buffering), commit_tool_results (handoff-aware result dispatch), project_task_list persistence to disk
provider.rs (1704) — raw TCP+rustls HTTP client: CompletionRequest (11 fields), ApiMessage (role/content/tool_call_id/tool_calls/cache_control/reasoning_content), ProviderEvent (Delta/Reasoning/ToolCall/Done/Error), 18 tool definitions (token-efficient JSON, built once), request body building (ReqMsg+RequestBody with serde skip_serializing_if, thinking/stream_options), SSE parsing (line-by-line, tool call accumulation by index with fix_args repair, content_filter detection, usage tracking), chunked transfer decoding (ChunkedReader implementing std::io::Read), counting API (OpenAI/Anthropic/OpenRouter/NVIDIA/generic via native_post, multi-field response parsing), `native_get` (rotating browser profiles—8 Chrome/Firefox/Safari profiles across Win/Mac/Linux, cookie jar with Set-Cookie parsing, web rate limit enforcement, chunked decode), `native_post` (extra_headers for x-api-key/anthropic-version), model list fetch (GET /v1/models), api_rate_limit (per-provider-model requests_per_hour enforcement), tls_config (OnceLock<Arc<rustls::ClientConfig>>, webpki-roots)
session.rs (171) — `ensure_session` (seed system prompt + sysinfo report + project context into empty session), `prepare_request_messages_for_session` (flush pending writes→load full history from JSONL disk→dedup by ID→strip orphaned tool_calls→estimate tokens for UI→convert to ApiMessage with cache_control on system message), `delete_session`
helpers.rs (981) — fuzzy find-replace (6 strategies: exact→CRLF-normalized→whitespace-normalized→tabs-normalized→anchored line match via similarity_score≥0.55→Myers DP sequence alignment with max_gap band), Levenshtein distance (O(n·m) with early exit for large length differences), Jaro-Winkler similarity, token-set similarity (BTreeSet intersection/union), combined_similarity, line-number stripping (detect "NN | " prefix from read_file output), tool error formatting (JSON {"error":..., "suggestion":...}), find_nearby_lines (diagnostic: show top-3 similar lines with context), is_incomplete_task_response (12 continuation-signal phrases), parse_todo_from_tool_args, parse_project_task_from_tool_args, project_context_string (project name/root/top-level entries + project task list)

## crates/fs/ — filesystem tools (~860 lines)
lib.rs (9) — crate root, module declarations
shell.rs (176) — background shell execution via channels (cmd on Windows with CREATE_NO_WINDOW|CREATE_NEW_PROCESS_GROUP + temp .cmd script, sh -c on Unix), stderr capture with separate named thread, PID reporting via channel, temp script cleanup with untrack, ShellEvent enum (Output/Done/SpawnError)
explorer.rs (460) — FsEntry (path/name/is_dir), gitignore-respecting list_dir (Gitignore struct: pattern/negated/dir_only/anchored rules, glob_match for anchored patterns, glob_match_segment for bare names), list_dir_all (no filtering, for UI), find_project_root (walk up for .gitignore/.git), glob_files (recursive walk, hidden-file skip, relative path output), grep_files (single-file fast path + recursive grep_walk with 1MB/binary limits, case-insensitive support, regex via core::helpers::matches_pattern), read_file (512KB cap for UI preview)
helpers.rs (216) — `extract_files`/`write_extracted_files` (code-fence ```filename.ext``` parsing with 72 known language tag exclusions + path-traversal protection), `glob_match` with `*`/`**`/`?` support (segmented match for `**` patterns), `glob_match_segment` (single-component wildcard matching)

## crates/ui/ — egui UI panels (~6,114 lines)
lib.rs (15) — crate root, module declarations
ui_chat.rs (2266) — chat panel: session tab bar (horizontal scroll, activity spinner, per-project tab accent color, close/reopen logic, closed-session dropdown), message bubbles (user right-aligned with configurable fill/stroke, assistant left-aligned with reasoning section), streaming rendering (live markdown + reasoning + live shell terminal with cursor blink), collapsible tool-result cards (19 structured renderers: read_file/read_entire_file/read_files/write_file/patch_file with unified diff/patch_lines/run_shell with terminal/list_dir/delete_file/rename_file/create_dir/grep/glob/web_search/fetch_url/todo_list/handoff/name_session), unified diff view (LCS algorithm for ≤2000 lines, simple greedy fallback beyond, context hunk grouping with "[...]" separators, line numbers with offset, copy button), scroll lock (auto-scroll to bottom on streaming, user-scrolled-up detection), lazy-load from disk (load_messages_before on scroll-to-top or button click, anchor-preserving prepend), per-project tab colors (hash-derived accent), terminal rendering with copy button, replay button (↺ to rewind and restart from a message), display buffer eviction (drop oldest when near bottom), keyboard navigation (arrow/page up/down), input row (multiline with Shift+Enter, Send/Stop button, thinking mode toggle, reasoning effort popup, todo/project-tasks toggle buttons), empty state
ui_settings.rs (1450) — 6 tabs: Providers (API key/base URL/models URL/model/context window/thinking API/handoff %/allow outside access/max output/max output thinking/rate limit, per-provider cards with enable/disable/set-active/reset/remove, model fetch from API, saved models list), Projects (project cards with root path, session list with inline rename/delete, delete all sessions), Prompt (system prompt editor + handoff trigger prompt editor, reset to defaults), Session (messages in RAM, completion delay, web rate limit, disk write rate), Timeouts (API & streaming: stream idle/request max/tool timeout/max retries/retry wait cap; Shell: default timeout/max timeout), About (version/renderer info, system info display, debug mode checkbox, inspection overlay checkbox, refresh sysinfo button, OpenGL detection with install guide, security warning); inline tab_btn widget, provider grid, project list with inline session rename/delete
ui_explorer.rs (921) — recursive file tree (shows all files including hidden, CollapsingHeader for dirs), file preview popup window (text editor with editable buffer + Save/Ctrl+S, image viewer for png/jpg/gif/bmp/webp via `image` crate, gutter line numbers painted on context layer for scroll-proof alignment, cursor-following auto-scroll), rename/delete context menu (right-click on files/dirs, inline TextEdit for rename), unsaved-changes confirmation dialog (Save & Close/Discard/Cancel), horizontal scrollbar for wide files, ephemeral viewer_scroll (never persisted to ron)
ui_toolbar.rs (371) — project picker (ComboBox with New Project...→rfd folder dialog), session picker (filtered by project, +Session button, closed sessions in dropdown), provider/model pickers (with manifest models + saved models), context-budget meter bar (color-coded: green→yellow→red), network blink-dot (spinner + bytes received), action buttons (Settings/Files/Handoff/Reasoning toggles with lit/unlit visual states), lit_btn helper (transparent button with accent border when active)
ui_todo.rs (303) — floating session task list window (header with title/Clear/X, progress bar with percentage, colored priority dots, auto-close + clear when all completed, empty state placeholder); uses shared todo_scroll_area from helpers
ui_project_tasks.rs (313) — floating project task list window (identical structure to ui_todo but for project-level tasks, persists via ProjectMeta JSON on disk)
helpers.rs (478) — `format_time` (UTC HH:MMZ), `extract_tool_summary` (19-tool result summary parsing with file/line/byte counts), `extract_tool_body`/`get_tool_body` (strip "Tool `X` result:\n" prefix), `parse_path_header` (new "filepath\n-- N lines, B bytes --\n" + legacy "path:..." formats), `parse_inline_formatting` (strip `code` and **bold** markers for plain-text), `append_rich_inline_to_job` (LayoutJob builder with code blocks + bold + italic spans, 50k iteration safety limit), screen pixel sampling (Windows GetPixel FFI via GetDC/GetCursorPos/ReleaseDC, COLORREF→[f32;3]), `section_heading`/`field_label`/`toolbar_separator` (reusable settings widgets), `todo_scroll_area` (shared scroll area with max 500px height, stick-to-bottom, generic item renderer + empty state)

**Total: ~18,740 lines of Rust source across 30 files (excluding `target/` and binary assets).**
