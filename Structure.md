# AutoCode — Workspace structure

**Mission — A lightweight, egui-based AI coding assistant.**
- Minimize RAM usage and binary size
- Keep codebase clean, organized, maintainable
- Essential features only, no bloat
- Prefer `std`; minimize deps

Cargo.toml (24) — workspace root, 5 crate members (autocode/core/ai/fs/ui), resolver = "2", version 0.2.1, release LTO/strip, panic=unwind

## crates/autocode/ — binary entry (~583 lines)
main.rs (51) — rustls crypto install, eframe::run_native (1400×900, Glow/Wgpu auto-select), data dir init, providers.json seeding
app.rs (527) — AutocodeApp (eframe::App): AppState + ChatRuntime map, panel wiring, frame update (sysinfo poll, pending writes, stale session purge, shell task cap at 200, folder picker), floating windows, toolbar/explorer/chat panels, debug inspection, save (sync session state, flush writes, persist project meta), exit (drain runtimes, temp file cleanup)
build.rs (4) — embed Windows icon resource
helpers.rs (1) — reserved

## crates/core/ — state, utilities, tokenizer (~5,626 lines)
lib.rs (21) — crate root, module declarations
state.rs (1500) — AppState, Project, ApiProvider, ProviderKind (OpenRouter/NvidiaNim/OpenAiCompatible/OpenCodeGo), ThinkingApi (Off/DeepSeek/OpenAI/Anthropic/Gemini/Grok), Session (max 50, with id/label/messages/next_message_id/created_at/provider/model/todo_list), ChatMessage (id/role/content/timestamp/token_count/tool_call_id/tool_calls/tool_meta/reasoning_content), SecretString (heap-zeroing on drop), TodoItem/TodoList, ProjectTaskList, ProjectMeta (versioned disk metadata), ShellTask/ShellStatus, PendingWrites (rate-limited disk batcher), embedded provider manifest (include_str!), DEFAULT_SYSTEM_PROMPT, DEFAULT_HANDOFF_TRIGGER_PROMPT, DEFAULT_HANDOFF_CONTINUATION_PROMPT, DEFAULT_CONNECTION_DROP_PROMPT, prune_disk_state, flush_pending_writes
helpers.rs (1439) — ID gen (5-char base36), token estimation (heuristic: word/symbol/CJK detection, ~3.2–4.0 chars/token; tiktoken via estimate_full_request_tokens), path resolution + traversal guard (resolve_path/resolve_path_write with cached variants, blocked_error, within_root), tiny backtracking regex engine (Pat enum: Lit/Any/Star/Plus/Opt/Class/AnchorStart/AnchorEnd, compile+match), serde defaults (16 functions), budget/usage display, sanitize_filename, update_full_estimate, panic_msg; extensive test suite
fsutil.rs (148) — exe_dir, \\?\ extended paths (Windows case-sensitivity fix), atomic read_to_string/write/metadata/read_dir/create_dir_all/remove_file/remove_dir/rename/is_dir/display_path, write_cmd_script (@echo off wrapper), TEMP_FILES tracking (OnceLock<Mutex<Vec<PathBuf>>>)
theme.rs (140) — dark Visuals+Style, Palette (20 colors: BG_BASE/PANEL/SURFACE/ACTIVE, ACCENT/ACCENT_DIM, TEXT_PRIMARY/SECONDARY/MUTED/CODE, BORDER, SUCCESS/WARNING/ERROR/PURPLE), hash-based project_accent (djb2→HSV→RGB), ROUND_SM(4)/MD(6)/LG(10)
extract.rs (298) — HTML scraping (scraper crate), DuckDuckGo result extraction (uddg URL decode, domain blacklist of 33 sites), GitHub content extraction (highlight blocks, markdown-body, repo-description), collect_text with tag skipping, search cache (LazyLock<Mutex<HashMap>>, 120s TTL, 500 max entries)
sysinfo.rs (689) — OS/CPU/GPU/RAM/shell/tool detection; Windows: raw Win32 FFI (kernel32 GetSystemInfo+GlobalMemoryStatusEx, advapi32 registry CPU name + PCI vendor GPU scan); Unix: /proc/cpuinfo, /proc/meminfo, sysctl (macOS), /sys/class/drm + lspci (Linux GPU); tool probing (14 tools: rg/grep/git/curl/python/node/cargo/npm/pip/make/docker/findstr/powershell); has_opengl check
session_storage.rs (629) — atomic JSON/JSONL session persistence (temp file + rename), prefix-based load/save/delete, orphan temp scavenge (.tmp_* cleanup >1h), repair_truncated_jsonl_line, sanitize_tool_calls (3 repair strategies), load_messages_before (lazy-load), truncate_messages_after (rewrite JSONL), project_meta_path/save_project_meta/load_project_meta (versioned ProjectMeta JSON), SessionMeta, save_session_meta
chunked_jsonl.rs (215) — Chunked JSONL message storage (1000 messages/chunk), chunk rotation, read/write/append for large session histories
persistence.rs (152) — Background persistence thread (PersistenceThread) for offloading JSONL writes from UI thread, rate-limited batched disk writes
provider_file.rs (225) — User-editable providers.json schema (ProviderEntry, ModelEntry: context_window, max_output_tokens, thinking_api, reasoning_efforts, supports_cache_control, requests_per_hour), save/load from AutoCode_data
shell_task_storage.rs (82) — Shell task persistence (save/load/list/delete individual shell tasks as JSON files with ShellTaskMeta)
tokenizer/mod.rs (88) — Tokenizer trait; TiktokenTokenizer (o200k/cl100k/p50k/gpt2 fallbacks by model family); HeuristicTokenizer fallback; `offline_token_count`

## crates/ai/ — AI provider client + chat orchestration (~6,461 lines)
lib.rs (13) — crate root, module declarations
chat.rs (3548) — orchestration: send_message, start_completion (pre-flight context check, rate limiting, partial response continuation), SSE stream poll (256 events/frame cap, exponential backoff), error classification (transient: 429/502/503/504/520/timeout/dns/tls→retry forever with 5s→180s cap; permanent: auth/quota/filter→surface), tool-call dispatch (20 handlers: read_file/read_entire_file/read_files/write_file/list_dir/project_tree/delete_file/rename_file/create_dir/grep/patch_file/patch_lines/web_search/fetch_url/todo_list/project_task_list/glob/handoff/name_session/run_shell), tool-call name inference, orphaned tool-call cleanup, auto-continuation (on incomplete tasks), auto-handoff (context threshold→trigger prompt), session auto-naming (80-char max), replay (truncate + restart), message-truncation (RAM eviction), live shell streaming (sequential, timeout, output buffering)
provider.rs (1712) — raw TCP+rustls HTTP client: CompletionRequest (11 fields), ApiMessage, ProviderEvent (Delta/Reasoning/ToolCall/Done/Error), 20 tool definitions (token-efficient JSON, built once), request body building (skip_serializing_if, thinking/stream_options), SSE parsing (line-by-line, tool call accumulation by index, fix_args repair, content_filter detection, usage tracking), chunked transfer decoding (ChunkedReader), counting API (OpenAI/Anthropic/OpenRouter/NVIDIA/generic via native_post), native_get (rotating browser profiles—8 Chrome/Firefox/Safari profiles, cookie jar, web rate limit), native_post (extra_headers), model list fetch (GET /v1/models), api_rate_limit, tls_config (OnceLock<Arc<rustls::ClientConfig>>, webpki-roots)
session.rs (168) — ensure_session (seed system prompt + sysinfo + project context into empty session), prepare_request_messages_for_session (flush writes→load full JSONL→dedup by ID→strip orphaned tool_calls→estimate tokens→convert to ApiMessage with cache_control), delete_session
helpers.rs (895) — fuzzy find-replace (7 strategies: exact→CRLF-normalized→whitespace-normalized→tabs-normalized→anchored line→Myers DP sequence alignment→single-line fuzzy fallback), Levenshtein distance (O(n·m)), Jaro-Winkler similarity, token-set similarity (BTreeSet intersection/union), combined_similarity, line-number stripping, tool error formatting, find_nearby_lines, is_incomplete_task_response (12 continuation-signal phrases), parse_todo_from_tool_args, parse_project_task_from_tool_args, project_context_string
thread_pool.rs (125) — Background thread pool for concurrent job execution with panic isolation via std::thread::spawn + catch_unwind

## crates/fs/ — filesystem tools (~880 lines)
lib.rs (8) — crate root, module declarations
shell.rs (199) — background shell execution via channels (cmd on Windows with CREATE_NO_WINDOW|CREATE_NEW_PROCESS_GROUP + temp .cmd script, sh -c on Unix), stderr capture with separate named thread, PID reporting, temp script cleanup with untrack, ShellEvent enum (Output/Done/SpawnError), shell output streaming to UI
explorer.rs (467) — FsEntry (path/name/is_dir), gitignore-respecting list_dir (Gitignore struct: pattern/negated/dir_only/anchored rules, glob_match for anchored patterns, glob_match_segment for bare names), list_dir_all (no filtering, for UI), project_tree (recursive up to depth 20, respects gitignore), find_project_root (walk up for .gitignore/.git), glob_files (recursive walk, hidden-file skip, relative output), grep_files (single-file + grep_walk with 1MB/binary limits, case-insensitive, regex via core::helpers::matches_pattern), read_file (512KB cap for UI preview)
helpers.rs (206) — extract_files/write_extracted_files (code-fence ```filename.ext``` parsing with 72 known language tag exclusions + path-traversal protection), glob_match with `*`/`**`/`?` support (segmented match for `**` patterns), glob_match_segment

## crates/ui/ — egui UI panels (~5,971 lines)
lib.rs (14) — crate root, module declarations
ui_chat.rs (2198) — chat panel: session tab bar (horizontal scroll, per-project accent color, close/reopen, closed-session dropdown), message bubbles (user right-aligned, assistant left-aligned with reasoning section), streaming rendering (live markdown + reasoning + live shell terminal with cursor blink), collapsible tool-result cards (20 structured renderers), unified diff view (LCS ≤2000 lines, greedy fallback beyond, context hunk grouping), scroll lock (auto-scroll on streaming, user-scrolled-up detect), lazy-load from disk, per-project tab colors, terminal rendering with copy, replay button, display buffer eviction, keyboard nav, input row (multiline, Send/Stop, thinking mode toggle, reasoning effort popup, todo/task toggle buttons)
ui_settings.rs (1535) — 6 tabs: Providers (API key/base URL/model/context window/thinking API/handoff %/allow outside/max output thinking/rate limit, per-provider cards with enable/disable/set-active/reset/remove, model fetch), Projects (project cards with root path, session list with inline rename/delete, delete all sessions), Prompt (system prompt + handoff trigger editors, reset to defaults), Session (messages in RAM, completion delay, web rate limit, disk write rate), Timeouts (API: stream idle/request max/tool timeout/max retries/retry wait cap; Shell: default/max timeout), About (version/renderer/system info, debug mode, inspection overlay, OpenGL detection, security warning)
ui_explorer.rs (857) — recursive file tree (all files including hidden, CollapsingHeader for dirs), file preview popup (text editor with editable buffer + Save/Ctrl+S, image viewer for png/jpg/gif/bmp/webp, gutter line numbers on context layer), rename/delete context menu, unsaved-changes dialog, horizontal scrollbar, ephemeral viewer_scroll
ui_toolbar.rs (340) — project picker (ComboBox + New Project→rfd folder dialog), session picker (filtered by project, +Session, closed dropdown), provider/model pickers (manifest + saved models), context-budget meter (green→yellow→red), network blink-dot (spinner + bytes received), action buttons (Settings/Files/Handoff/Reasoning toggles), lit_btn helper
ui_todo.rs (285) — floating session task list (header with title/Clear/X, progress bar with percentage, colored priority dots, auto-close + clear when all completed, empty state); uses shared todo_scroll_area from helpers
ui_project_tasks.rs (297) — floating project task list (identical structure to ui_todo but for project-level tasks, persists via ProjectMeta JSON on disk)
helpers.rs (445) — format_time (UTC HH:MMZ), extract_tool_summary (20 tools, file/line/byte counts), extract_tool_body/get_tool_body, parse_path_header (new "filepath\n-- N lines, B bytes --\n" + legacy "path:" formats), parse_inline_formatting, append_rich_inline_to_job (LayoutJob builder, 50k iteration limit), screen pixel sampling (Windows GetPixel FFI), section_heading/field_label/toolbar_separator, todo_scroll_area

**Total: ~19,520 lines of Rust source across 35 files (36 including test, excluding `target/` and binary assets).**